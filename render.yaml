services:
  - type: web
    name: terabox-proxy
    env: docker
    plan: free
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: PROXY_BASE
        value: "https://streaming-bot-1.onrender.com/proxy?url="
